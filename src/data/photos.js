// Статичные фото из public/. Фото сортов, статей и рецептов здесь больше не
// хранятся: их заливают через админку и формы в Supabase Storage
// (см. services/_photo.js), а в БД лежит короткий публичный URL.
//
// hero — фон главной. Путь продублирован в index.html (<link rel="preload">):
// при смене файла или имени меняй его в ОБОИХ местах.
export const PHOTOS = {
  hero: '/hero.webp',
};
